export interface NotificationSettings {
  sessionReminders: string[];
  newsletterAndPromos: string[];
  accountActivity: string[];
}
export interface Stat {
  title: string;
  value: number | string;
}
