service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth!=null;
    }

    match /images/sessions/{session} {
      match /thumbs/{thumb} {
       allow read: if true;
      }
    }
  }
}
