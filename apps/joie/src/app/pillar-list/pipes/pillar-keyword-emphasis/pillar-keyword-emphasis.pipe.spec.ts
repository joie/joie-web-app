import { PillarKeywordEmphasisPipe } from './pillar-keyword-emphasis.pipe';

describe('PillarKeywordEmphasisPipe', () => {
  it('create an instance', () => {
    const pipe = new PillarKeywordEmphasisPipe();
    expect(pipe).toBeTruthy();
  });
});
