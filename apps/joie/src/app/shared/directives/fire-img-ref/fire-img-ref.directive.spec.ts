import { FireImgRefDirective } from './fire-img-ref.directive';

describe('FireImgRefDirective', () => {
  it('should create an instance', () => {
    const directive = new FireImgRefDirective();
    expect(directive).toBeTruthy();
  });
});
