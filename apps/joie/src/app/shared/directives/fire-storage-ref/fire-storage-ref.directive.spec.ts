import { FireStorageRefDirective } from './fire-storage-ref.directive';

describe('FireStorageRefDirective', () => {
  it('should create an instance', () => {
    const directive = new FireStorageRefDirective();
    expect(directive).toBeTruthy();
  });
});
