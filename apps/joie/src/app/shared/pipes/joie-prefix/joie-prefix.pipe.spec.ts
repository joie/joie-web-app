import { JoiePrefixPipe } from './joie-prefix.pipe';

describe('JoiePrefixPipe', () => {
  it('create an instance', () => {
    const pipe = new JoiePrefixPipe();
    expect(pipe).toBeTruthy();
  });
});
